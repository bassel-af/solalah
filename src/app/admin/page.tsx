/**
 * /admin — placeholder page for the platform owner dashboard.
 *
 * Metrics, charts, and queries land in a follow-up task. For now this
 * proves the gating + routing scaffold works end-to-end.
 */
export default function AdminDashboardPage() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>لوحة تحكم المنصة</h1>
    </main>
  );
}
